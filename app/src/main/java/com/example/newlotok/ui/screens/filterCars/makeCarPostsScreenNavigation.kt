package com.example.newlotok.ui.screens.filterCars

import androidx.compose.runtime.LaunchedEffect
import androidx.compose.ui.platform.LocalContext
import androidx.navigation.NavGraphBuilder
import androidx.navigation.NavHostController
import androidx.navigation.compose.composable
import com.example.newlotok.ui.TokensViewModel

/*
fun NavGraphBuilder.makeCarPostsScreenNavigation(
    tokensViewModel: TokensViewModel,
    navController: NavHostController,
){

    composable(route = "carPostsScreen"){
        val context = LocalContext.current
        LaunchedEffect(Unit) {
            tokensViewModel.getCarPosts(make = tokensViewModel.make)
        }
        FilteredCars(
            tokensViewModel = tokensViewModel,
            navController = navController,
            getCarPostsUiState = tokensViewModel.getCarPostsUiState,
            retryAction =
        )
    }
}
 */