package com.example.newlotok.ui.components.topBar

/*
@Composable
fun AllTopBars(currentScreen: LotokScreen){
    when(currentScreen.topBarType){
        TopBarTypes.TopBarMenuLogNotification -> {
            TopBar(
                startIcon = { StartIconMenu() }, //StartIconMenu()
                topBarCenter = { TopBarCenterLogo()},   //TopBarCenterText(text = "Home")
                endIcon = { EndIconNotification() }
            )
        }
        TopBarTypes.TopBarGoBackTextProfile -> {
            TopBar(
                startIcon = { StartIconGoBack() }, //StartIconMenu()
                topBarCenter = { TopBarCenterText(text = "Select your car") },   //TopBarCenterText(text = "Home")
                endIcon = { EndIconProfile() }
            )
        }
    }
}
 */